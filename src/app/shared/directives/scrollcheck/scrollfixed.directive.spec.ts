import { ScrollfixedDirective } from './scrollfixed.directive';

describe('ScrollfixedDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollfixedDirective();
    expect(directive).toBeTruthy();
  });
});
