import { CloseCanvasDirective } from './close-canvas.directive';

describe('CloseCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
